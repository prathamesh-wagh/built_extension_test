var Classes         = require('./controller/classes')
var ApplicationUser = require('./controller/application-user')

module.exports = {
	"/v[0-9]":{
		before: function function_name(req, res) {
			//Non sense
			req.isExtension = true
			return utils.Promise.resolve()
		},
		"/classes"    : Classes,
		"/application":{
			"/users": ApplicationUser
		}
	}
}